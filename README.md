# Piecebook

Phone-first web app for One Piece TCG collectors. V1 lists every EN booster set on Cards' roster with its box priced daily, lets you search every set at once by name or number (a full number opens the card), browse every set's EN checklist by rarity, search a set by name or number, sort by name or seed price, read a short intro per set, see every print of a character across sets, see which sealed product feeds each set, mark the cards you own, log what you paid, and see a seed-priced portfolio.

Specs live alongside the code: `V1-PRD.md` (scope), `V1-DESIGN-PACK.md` (IA, palette, copy), `docs/V1-DESKTOP-LAYOUT.md` (tablet/desktop breakpoints), `docs/V1-SETTILE-AND-GRID-PRICES.md` (sealed box prices on set tiles, muted USD on card cells), `docs/V1-OWN-AND-SET-DEPTH-UI.md` (set intro, thin Own), `docs/V1-SET-DETAIL-CONTROLS.md` (search, sort, All tab), `docs/V1-COST-AND-PORTFOLIO-UI.md` (cost basis, Portfolio), `docs/V1-SETS-ROSTER-UI.md` (full EN roster, pending set detail), `docs/V1-MOVERS-COMPLETION-PLAYSETS-UI.md` (movers, change since starred, set completion, playsets), `docs/V1-BOX-CARD-AND-ART-UI.md` (the box as a product card, box art), `docs/V1-DECKS-UI.md` (decks: the fourth tab, the builder, the collection gap, legality), `docs/V1-STARTER-DECKS-LINKS-LEARN-UI.md` (starter decks, the Limitless link, learn to play), `docs/V1-FIND-UI.md` (search facets, artists, characters, upcoming sets), `docs/V1-BELONG-UI.md` (set stories, the release timeline, Start here), `docs/V1-FANDOM-UI.md` (wiki who-is line, On This Day, countdown; Today's card stood down), `seed-sources.md` (data provenance), `docs/HANDOFF.md` (how this project is run, for a fresh session).

## Run it

```bash
npm install
npm run dev
```

Open the printed URL (default `http://localhost:5173`). Below 768px the app is a phone column with a bottom tab bar (Sets · Collection · Decks · Portfolio); from 768px up it grows to a 720 / 1100 / 1280px column with a quiet top bar and a 3–5 column card grid (see `docs/V1-DESKTOP-LAYOUT.md`).

```bash
npm run build     # type-check + production build into dist/
npm run preview   # serve the production build locally
```

## Routes

| Route | Screen |
| --- | --- |
| `/` | Sets — the 23 EN booster sets as tiles, oldest EN release first, then the sets coming next (discovered daily from TCGCSV, code read from the group's card numbers: `EN SET · OP-18`, `Coming 20 Nov 2026`, `Box · pre-order US $ · as of`), each with the box's official TCGplayer render and its daily market price; under them the 36 starter decks (ST-01…ST-36) as rows in number order with a jump link from the top: render, `ST-23 · RED Shanks`, `you own 1 · 6 Jun 2025 · 15 different cards · US $48.54`; a quiet `Start here ›` row closes the page. Two text tabs right of the title switch the view: **Tiles** (this) or **Timeline** (`/?view=timeline`): every booster, extra booster and premium booster in English release order under year rules with a Today rule before the sets still to come, On This Day under the rule (the short date, then at most three 44px birthday rows A–Z), each row a link reading `OP-02 · Paramount War` (`12 of 154` at the edge once started), `JP 4 Nov 2022 · EN 10 Mar 2023 · 4 months later` (`US date 20 Nov 2026 · in 75 days` where the date is TCGplayer's; `today` / `tomorrow` only on a Bandai day), and the JP name; the view lives in the URL only. One search field (`Search cards, sets, artists`, `?q=`) searches every set at once and reads typed words as facets — colour (any-of), type, `cost 4`, trait, the eight effect keywords (a keyword counts only where the card has it), `owned`, `missing` — shown as lit chips on one wrapping line with `Colour · Type · Cost · Trait` openers; the reading in words leads the results (`62 red Blockers you don't own`), grouped by home set in roster order (ten per set, then “All N in OP-09”); a set code shows the set, an illustrator's name an Artists group, names that recur across sets sit under “Across sets”, and a full card number shows an “Open” row — Enter on it opens the card |
| `/sets/:setCode` | Set detail — every roster set is seeded: under the set's name its story in one sentence of ours (`The war at Marineford reaches the game, and black, which the Absolute Justice starter deck introduced, joins the boosters.`), the dates `JP 4 Nov 2022 · EN 10 Mar 2023`, and `121 cards · 154 prints with alternate arts` (Bandai's figure against the prints we hold); then the box as a product, search (`?q=`), sort (`?sort=`), rarity tabs with All first (`?rarity=`), card grid (e.g. `/sets/OP-09?sort=price-desc`, `/sets/OP-09?q=roger`); `You own 6 of 151` under the set name once a card from the set is owned (the tile on Sets home says `· you own 6`). The box as a product: official render, TCGplayer market with its date and movement, packs · cards per pack · JP box title (EB-04: “No EN box …”). A starter deck's page reads `EN starter deck`, files every print by rarity with the leader first, counts `different cards`, and offers **Open in Decks** (leader plus one of each card) and **I have this deck** An upcoming booster (EB-05, OP-18) shows `US release 20 Nov 2026 · TCGplayer's date · in 75 days` in the header (the countdown never says today or tomorrow on a TCGplayer day). A roster set whose row is flipped back to `pending` shows sealed notes / EN date, then “Checklist not seeded yet”. Unknown code: “Set not found” |
| `/cards/:cardNumber` | Card detail — star to watch; `Illustrated by …` where Limitless credits an artist; a **Decks that play this card** row linking to Limitless's page for the card (their latest tournament lists), link only; Market (seed) with a price-history line once two or more dated points exist; the Play block (category · colour · cost or life · power · counter · attribute, traits, effect and trigger text as printed, `Block 3 · Legal in Standard and Extra`); Mark owned / Remove from collection; once owned, Add cost basis / Edit cost / Clear cost (e.g. `/cards/OP09-001p1`) |
| `/characters/:name` | Character view — under the name, the gated One Piece Wiki sentence in ink when the title is not a later-name reveal (`Dracule Mihawk is a world-famous pirate…`), then `Born 9 March` when the Char Box parsed a day, then the prints line, then `From One Piece Wiki, CC BY-SA 3.0.` only if a line printed (licence link only; no wiki article link; nothing on a birthday-only page; Imu shows neither). Then every print carrying that name, grouped by home set in roster order, with the owned check and watch star on each cell (`/characters/Shanks`), and a star for the name itself: a watched character shows in Watching as its biggest mover since you starred, named and priced. Reached from “Across sets” in search and from `All prints of …` on a card |
| `/decks` | Decks — every deck you have built, newest first (leader art · name · colour · count · legality); one primary **New deck** |
| `/artists/:name` | Illustrator — every credited print, newest set first, `122 credited prints across 35 sets · you own N`; spelling variants folded. Reached from `Illustrated by` on a card and from search |
| `/learn` | Learn to play — five sentences of ours (checked against Bandai's comprehensive rules), then Bandai's rules page, overview sheet, rule manual, comprehensive rules and official channel as outbound rows. Reached from Decks home, the builder and every starter deck's page |
| `/decks/:id` | Deck builder — leader, a check that says in words what is wrong (size, copies over four, cards outside the leader's colours, Standard / Extra legality as Limitless publishes it), the fifty by category with type-a-number steppers, Add cards filtered to the leader's colours with **Standard only** and **Owned** chips, From your collection (`You own 11 of 51 · US $9.02 to complete`, star the gap), text in and out in the `4xOP09-004` form, a pinned count · legality · Copy strip with one-step Undo |
| `/collection` | Watching (starred sets and cards; each watched card shows `−$1.35 since 17 Aug 2026`, its seed-price change since the day it was starred), then owned cards with a muted `Paid …` line where a cost exists; **All · Playsets** chips on the Owned heading, Playsets listing cards held four or more times |
| `/portfolio` | Market value, Cost basis, Unrealized P/L from seed prices; chips `US $` / `S$` at the title (remembered in `piecebook.displayCurrency.v1`, default US $); on S$ the market and Top owned are the USD seed at the dated European Central Bank cross (`S$1.2669 to US $1 · European Central Bank · 4 Sep 2026`), costs stay in the currency paid, and P/L stays in US dollars on both tabs; **Movers** (biggest Week / Month seed-price moves among owned and watched cards, up to five up and five down, each with its since date; moves under $0.10 left out, always US $); Top owned |
| `/about-prices` | How prices work — what “seed” means, where card, history and box prices come from, what Piecebook never does. Linked from the chart footnote, Portfolio, and Data health |
| `/data` | Data health — warnings this week, named unpriced prints, what was left out on purpose (including SG asks), then catalogue counts and calendar dates. Reached from How prices work. Every figure is a count from the files; a gap is named, not filled |
| `/start` | Start here — the wordmark and three doors, Collect (`/`), Play (`/decks`), Belong (`/?view=timeline`), each a pillar word and one sentence; `New to the game? Play has the rules in five sentences.` (→ `/learn`), the trust line (→ `/about-prices`) and `Just show me the sets`. Shown once: a first visit to `/` with nothing owned redirects here until a door or Skip sets `piecebook.started.v1`; deep links never redirect; nothing lights in the tab bar |

## Data

- **Catalog** is the read-only seed under `data/`: one `{code}-en-seed.csv` per roster set (58 files: 23 booster sets and 35 starter decks, 4,083 rows; see `docs/seed-sources.md`). Every `*-en-seed.csv` is picked up by `import.meta.glob`, bundled into its own `seed` chunk at build time and parsed in the browser. Regular sets load same-set numbered rows only (a row's `card_number` must carry its `set_code`, e.g. `OP17-001` under `OP-17`); PRB premium boosters and ST starter decks are reprint products and list every print in the box, so a card has one home set but can also appear in a PRB or ST checklist. See `data/SEED-VERSION.txt`. Adding a set is a CSV drop-in plus flipping its roster row to `ready`.
- **Prices** are the seed `market_usd` values with their `as_of` date. There is no live price feed; every price in the UI is labelled as seed. Card cells show the bare `market_usd` muted under the name; card detail carries the `as_of` date.
- **Parallels** are detected from the card number (`OP09-001p1`) and shown in their own Parallels tab; the All tab includes them too. Each parallel carries a human `variant` name from Limitless ("Alternate Art", "Manga Art", "Special Card", …) shown after the card name; the number stays visible.
- **Decks** run on the same card attributes: when a Decks screen opens, every set's attributes load (about 100 kB gzipped over the 23 per-set chunks) so the builder can tell colours, categories and Standard / Extra legality for any card; the main bundle is unchanged. Deck rules are Bandai's: one leader, exactly fifty, four copies of a number (a parallel is the same card), colours matching the leader, no leader in the fifty; nothing else is inferred.
- **Card attributes** live in `data/card-attributes/{code}.csv` (category, colour, cost / life, power, counter, attribute, traits, effect and trigger text, illustrator, regulation block, Standard / Extra legality), written by the same refresh and loaded lazily per set on card detail (`src/data/attributes.ts`). They feed the Play block and the `Illustrated by` line. Card text is Bandai's, shown as every card database shows it (accepted 5 Sep 2026); bracketed keywords such as `[On Play]` get a little weight, the words are unchanged. Where Limitless has no value (a blank, a `?`, or an untranslated `card.*` key) the line is omitted; nothing is inferred. 282 prints have no effect text and show none; 859 have no artist credit.
- **Data health** is `data/health.json`, written by the same refresh: counts, named unpriced prints, named exclusions. The `/data` page is the weekly read. `seed:check` warns if the file is more than 8 days old.
- **Wiki lines** come from `data/wiki/lines.json`, written by `npm run wiki:refresh` (`scripts/wiki-refresh.mjs`): One Piece Wiki text, CC BY-SA 3.0, two parse calls per Character or Leader name, never an image. Each entry carries the wiki title, a revid permalink, a birthday when the Char Box parsed one, a spoiler-gated first sentence (debut-arc cutoff), and the cleaned clauses. A redirect that adds a later name (`Imu` → `Nerona Imu`) is a reveal and the app shows nothing. First cache 6 Sep 2026: OP-01, OP-09, OP-17 and every leader (214 asked, 188 mapped, 40 lines, 169 births). `seed:check` fails a line over 22 words and warns after three silent days. See `docs/seed-sources.md`.
- **Set intros** come from `data/set-intros.json`: for every set the EN release date (Bandai EN product page), JP release date and Japanese title (Bandai JP product page), Bandai's EN card-types count, and packs-per-box / cards-per-pack where Bandai states them. Missing fields are simply omitted; nothing is inferred. **Set stories** (7.1) are the one piece of authored copy per set: `introTheme`, one sentence of ours written from the Bandai EN product page named in `introSource` (Bandai's EN card list for EB-04 and OP-17, which have no EN page; OP-18 has no line yet), never quoted, at most 22 words, present tense, no rarity / alt-art / campaign / anniversary words, no superlatives, and naming every one of the set's leaders or none. The refresh never writes one; `seed:check` warns on a ready set without a line and fails a line that breaks a rule. The story shows on the set page only, under the set's name.
- **Search and sort** on Set detail run in the browser over the seed. Search matches name or number (`004`, `op09-004`, `OP09-004`). Sort is Name A to Z (default), Name Z to A, Price high to low, Price low to high; cards without a seed price sort last. Search and sort live in the URL as `?q=` and `?sort=`.
- **Global search** on Sets home (`src/lib/search.ts`) parses facets first (`parseQuery`: colours, types, cost, traits, keywords via `hasKeyword`, owned/missing; attributes load once on first search and are cached) and then runs the same name-or-number match over every distinct card once (3,301; a PRB reprint of a home-set card is one card, a PRB-only parallel has PRB as its home set). Results are grouped by home set in roster order; a name that recurs in more than one set is listed under “Across sets” with its print and set counts and links to the character view. A query that is a full card number — hyphens, spaces and case forgiven (`OP09-118`, `op09 118`, `op09118`, `op09-004p1`, `P-014`) — resolves to that card: an “Open” row appears and Enter opens it. Under two characters nothing is searched (“Keep typing to search every set.”). Nothing is indexed ahead of time; 3,301 cards filter in under a millisecond per keystroke.
- **Sealed guidance** (the EN / JP box lines on set detail) is derived in `src/data/sealed.ts` from the intro dates and JP title: `Box feeds this set · 13 Dec 2024` / `Box (新たなる皇帝) · 31 Aug 2024`. EB-04 overrides the EN line because it has no EN box.
- **Sealed prices and box art** are automated (decision 3.1 and the box-card addendum, 5 Sep 2026). Every EN booster box's TCGplayer market price is pulled once a day by the refresh from [TCGCSV](https://tcgcsv.com), a public daily mirror of TCGplayer's own API, into `data/box-price-history.csv` (`set_code,as_of,market_usd,low_usd,source`); tiles and the box card show the newest row with its date, and from the second day the movement since. Each roster row names its box by `tcgplayerProductId` / `tcgplayerGroupId`; the same id gives the box image, TCGplayer's official product render, hotlinked from their CDN like card art from Limitless's (`…/product/{id}_400w.jpg` on tiles, `_in_1000x1000.jpg` on the card). Nothing is vendored or hand-read any more; the roster's own `usMarketUsd` / `asOf` are a fallback only and its `sgAskSgd` values are kept as provenance but not shown. `seed:check` warns if the feed goes quiet for 3 days.
- **Sets roster** is `data/sets-roster-en.json` (Cards): `{ asOf, note, sets }` with the 23 EN sets (OP-01…OP-17, EB-01…04, PRB-01…02; EB-04 has no EN box and carries null prices), each with `product: booster_box`, EN release date, `cardSeedStatus` (`ready` / `pending`), the EN booster box TCGPlayer US market price with its `asOf` date and `usSource` URL (all 22 sets), an SG ask only where verified (OP-09, OP-16), and optional box art. The roster decides which tiles appear on Sets home and in what order; the card CSVs only decide whether a set has a browsable checklist. `boxArtUrl: "vendored"` means “use the front already under `public/box-art/`”; anything that is not a local path is treated as no art.
- **Price history** is `data/price-history/{code}.csv` (`card_number,as_of,market_usd,source`, one row per card per day). Two sources, both labelled on the chart: `limitless` rows are the daily seed readings from `npm run seed:refresh`; `tcgplayer` rows are a one-off backfill of about a year of weekly market prices from TCGPlayer's product-page chart data (`npm run seed:backfill`, keyed by `data/tcgplayer-products.csv`, approved 5 Sep 2026). Where both exist for a day the Limitless reading wins. History is loaded lazily per set on card detail only and drawn as a quiet line with 1M · 3M · 6M · 1Y · All range chips. There is still no live feed.
- **Refreshing the seed**: `npm run seed:refresh` (Node 18+, no dependencies) re-pulls every roster set from Limitless with the rules in `docs/seed-sources.md`, rewrites the CSVs, appends today's prices to the history and repins `data/SEED-VERSION.txt`. `--sets OP-17,EB-04`, `--dry-run`, `--as-of YYYY-MM-DD`, `--no-history`. It never edits Cards' roster JSON. `npm run seed:check` validates the data contract (headers, same-set rule, rarity codes, CDN-only images, no checklist gaps, pinned counts, one history row per card per day). Parsing all 23 CSVs at startup costs ~4 ms; the seed chunk is ~55 kB gzipped.
- **Automation** (`.github/workflows/`): `ci.yml` type-checks, builds and runs `seed:check` on every push to `main` and every pull request. `seed-refresh.yml` runs the refresh daily at 02:10 UTC (10:10 SGT), builds against the result and opens or updates one PR on the `automation/seed-refresh` branch for review; it can also be run by hand from the Actions tab with a set list or as a dry run. Nothing lands on `main` unreviewed. One-time repo setting: Settings → Actions → General → Workflow permissions → allow GitHub Actions to create pull requests.
- **Where the history comes from**: Piecebook's own daily readings began on 4 Sep 2026. There is no free historical feed (Collectr and PriceCharting hold years because they have snapshotted licensed TCGPlayer / eBay sales data daily for years; PriceCharting's API explicitly excludes history), so on 5 Sep 2026 Jib approved a single backfill from TCGPlayer's product-page chart endpoint, which gives roughly a year of weekly market prices per print. The daily workflow carries it forward from there. Details and the trade-offs in `docs/V1-WATCHLIST-AND-CHARTS.md`.
- **Movers and change since starred** reuse the dated seed points: the reference for a Week / Month mover is the newest point at least that old (with weekly backfill points that can be up to 13 days), so every figure carries its `since` date; a watched card's change is measured from the newest point on or before the day it was starred. Nothing is shown for cards without two dated points, and zero moves stay silent.
- **Playsets** count a card's owned copies against the four a deck may run (`2 of 4 for a playset`, `Playset complete`); leaders (rarity `L`) are one per deck and are left out.
- **Portfolio math** runs in the browser over your owned cards: market value is Σ seed `market_usd × qty` (USD); on the S$ tab that sum is read at the dated ECB cross in `data/fx-usd-sgd.json` (EUR/SGD ÷ EUR/USD from the same daily table, never invented); cost basis is summed per currency (SGD, USD) and never converted; unrealized P/L is only computed for cards with a USD cost, against the USD seed market, and stays in US dollars on both tabs. Nothing is live. The daily refresh pulls the ECB table; `seed:check` fails a missing rate or one older than 4 weekdays.

## Your data is local-only

Owned flags, quantities and cost basis are stored in your browser's `localStorage` under the key `piecebook.v1`, one entry per owned card: `{ qty, ownedAt, cost?: { amount, currency: 'SGD' | 'USD', paidOn, note? } }`. A cost can only exist on an owned card; removing the card from your collection clears it. The watchlist (starred cards and sets) is stored separately under `piecebook.watchlist.v1`, and decks under `piecebook.decks.v1` (`{ decks: [{ id, name, leader, cards: { [baseNumber]: copies }, createdAt, updatedAt }] }`, base card numbers only). `piecebook.started.v1` records only that Start here has been seen. `piecebook.displayCurrency.v1` remembers the Portfolio `US $` / `S$` chip (`usd` or `sgd`; default `usd`). There are no accounts and nothing is sent anywhere. Clearing site data for the app wipes all of it.

## Deploying

The build in `dist/` is a static single-page app. Configure your host to serve `index.html` for unknown paths so deep links like `/cards/OP16-001` work (a `public/_redirects` file is included for Netlify-style hosts).

**GitHub Pages** (`https://najibjuraimi-glitch.github.io/piecebook/`): `.github/workflows/pages.yml` builds every push to `main` with `PIECEBOOK_BASE=/piecebook/` (Vite `base`, the router `basename` and the box-art path all follow it), copies `index.html` to `404.html` so deep links load, and commits the result to the `gh-pages` branch. One-time repo setting: Settings → Pages → Build and deployment → Source "Deploy from a branch" → `gh-pages`, `/ (root)`. Locally the base stays `/`.

## Not in V1

Live prices, alerts, invented FX rates (Portfolio S$ is a dated ECB reading, not a live quote), player search facets (colour, category, cost, trait, effect — growth map 2.3, shipped), scanning, accounts or sync, marketplace or affiliate links, dark mode.
