# Piecebook — how this project is run (handoff for a fresh session)
As of: 6 Sep 2026 SGT. Owner: Jib. Read `docs/OBJECTIVE.md` first (objective, pillar matrix, decisions taken), then `docs/growth-map.csv` (every task with dependencies; ClickUp mirrors it).

## The loop, per phase (Jib's rule: design first, then build)
1. **Draft** a design addendum in `docs/V1-*.md` (same front matter as the others) and render mockups from the real components at phone 390 and wide 1280. Post the note in chat with the mockups in the run's artifact folder (`/opt/cursor/artifacts/draft_phase<N>_*.png`) and a numbered list of decisions.
2. **Research**: ten simulated users as parallel subagents, each a distinct profile, each returning first impression · top 5 frictions with severity · 3 things that work · one trade. Always seat a domain expert (a rules judge caught three rules errors and a wrong link target in Phase 6b) and one non-English-market user. Ten is the number; fifteen only repeated. Synthesize by count-of-ten; check any factual claim a reviewer makes before acting on it.
3. **Revise** the draft (prototype + spec with a "Research → response" table). Keep agent jobs small and bounded — code, then renders, then spec — each committing or writing a file; never one open-ended agent for all three. Run coding agents in the foreground with a progress file (`/tmp/<job>-progress.txt`): background coding agents stalled twice (40 minutes; two hours with no output), while short read-only reviewer agents in the background are fine. Two coding jobs can run in parallel in separate worktrees when their file lists do not overlap; the maintainer merges them and does the visual QA.
4. **Approval** from Jib in chat ("approved"), then **build**: a fresh branch off `main` (or stacked on the unmerged branch it depends on), one commit per growth-map task, the addendum marked approved, README updated, a draft PR with screenshots committed under `docs/review/pr<N>/` and embedded inline, a "Review locally" line, direct links to the screenshots in the chat message. Never merge; Jib says "merge N" and the maintainer merges with `git merge --no-ff` and pushes `main`.
5. **ClickUp** lives at Team Space › Piecebook. Lists are Phase 0–9 (shipped product sequence), then Ongoing (standing) and Later · Southeast Asia (parked). Do not number standing or parked work as phases — that is the hop. Sync via the ClickUp MCP. Known ids are in `docs/growth-map.csv`. Growth map page: pinned doc › Growth map. Do not open 9.2+ rooms, Collect 12.x, or Play 13.x unless Jib asks.

## Non-negotiables
- Quiet paper, ink, one accent only on the primary action, no marketplace chrome, no percent badges, copy in plain words, no invented data: if a source does not publish it, say so rather than guess.
- **Nothing manual.** Data comes from automated sources or not at all: Limitless (cards, attributes, prices), TCGCSV (`tcgcsv.com`, a once-a-day mirror of TCGplayer's API: boxes, starter decks, prices, renders; identify with `User-Agent: Piecebook/1.0`, one pull a day, 120 ms between requests). TCGplayer's own site is never read by the workflow. Hand-read figures (the SG asks) were retired.
- `npm run build` and `npm run seed:check` must pass; CI runs both on every PR.
- Branch names `cursor/<topic>-eb1f`, lowercase. Do not force-push or amend. Screenshots: phone 390 (deviceScaleFactor 2) and wide 1280, headless Chrome via playwright-core at `/usr/local/bin/google-chrome`, against `npx vite preview`.

## Where things are (5 Sep 2026)
- Live: https://najibjuraimi-glitch.github.io/piecebook/ — every merge to `main` republishes via `.github/workflows/pages.yml`. Daily data: `.github/workflows/seed-refresh.yml` at 02:10 UTC opens a PR on `automation/seed-refresh`; Jib merges it (0.4).
- Shipped: Phases 0–4 and 3 complete; Phase 5 decided (5.1 closed, 5.4 re-opened as an automated dated S$ rate, 5.2/5.3 parked); Phase 6 complete (PRs #19, #20, merged 5 Sep). Phase 7a — Find (2.3 facets, 7.3 artists, 7.4 characters, 7.5 upcoming sets) built 6 Sep after a ten-reviewer round (`docs/V1-FIND-UI.md`); set codes for upcoming sets are read from TCGCSV's card numbers, not guessed.
- **Phase 7b — Belong (7.1 set stories, 7.2 release timeline, 8.1 Start here): merged 6 Sep 2026 as PR #22** (`6b50d22`, `docs/V1-BELONG-UI.md`, approved 6 Sep). Story lines are the only authored copy besides Learn to play: one sentence per booster set from Bandai's EN page, recorded with `introSource`, every rule enforced by `seed:check`. OP-17's Bandai EN page is a title-only stub and EB-04 has none, so both cite Bandai's EN card list; OP-18 has no line until Bandai publishes a page.
- **"Fandom, distilled" (7.7 wiki pipeline, 7.8 Who is / Born, 7.9 On This Day, 7.10 Today's card stood down, 7.11 countdown): merged 6 Sep 2026 as PR #23** (`ee0efaa`, `docs/V1-FANDOM-UI.md`, approved 6 Sep). Character pages carry the gated wiki sentence and `Born 9 March` (attribution only when a line printed; Imu shows nothing). The timeline Today rule carries On This Day birthdays and upcoming rows count down from the device date. Today's card is specified and not imported on the timeline. Wiki images are never pulled; wiki article links are never rendered.
- **5.4 currency + 9.1 data health: merged 6 Sep 2026 as PR #26** (`5dd8f48`, `docs/V1-CURRENCY-AND-HEALTH-UI.md`, approved 6 Sep). Portfolio chips `US $` / `S$` (remembered; default US $); S$ is the dated ECB cross, costs never convert, P/L stays in US dollars. `/data` names warnings, unpriced prints, exclusions and catalogue dates. 7.6 events stays low. Next by the map: 7.6 stays low; later demand and hypothesis 10.0 (MY/ID) wait on research.
- **Set singles (default Price · high to low + week move): merged 6 Sep 2026 as PR #28** (`fabcd10`, `docs/V1-SETTILE-AND-GRID-PRICES.md`). Default sort on set detail is Price · high to low; each cell shows the seed price in ink and the week move in dollars and percent, in ink, from the set’s price history. No red/green, no qty/add chrome.
- **Fandom page (Phase 9.1): merged 6 Sep 2026 as PR #29** (`docs/V1-FANDOM-PAGE-UI.md`, approved 6 Sep). `/belong` is the fandom home. Fifth tab **World**. Story is an index (titles and chapter/episode ranges — no plot). People and Fruits are the stored log (fruit and debut when we have them). Fruit rows stay text: names, type, eaters — no picture. Default cutoff is Debut arc; fruits closed until a saga chip; on a saga chip, a name with no stored debut is hidden. ClickUp 9.1: https://app.clickup.com/t/86eyuqatu. Next by the map: 7.6 stays low; later demand and hypothesis 10.0 (MY/ID) wait on research. No 9.2 until Jib asks.
- Later items with user demand from research: a Keyword opener chip (`[When Attacking]`, `[Activate: Main]`, `[DON!! x1]`); the cheapest playable print in a watched character's row (players) and new-print alerts (completionists); pre-order price history on a pending set's page; a star and Watching row for artists; sealed ownership in Portfolio; a JP layer (titles, dates); share-by-URL for decks; colour words for decks named without one; Bandai card Q&A link.
- **Hypothesis, parked (Jib, 6 Sep 2026):** a later phase *may* expand to Malaysia and Indonesia — ringgit, rupiah, and language translation. Map row 10.0 is Later, not a phase you finish before the fandom page. Seat collectors in those markets before opening a currency chip or a locale. Not work to start: https://app.clickup.com/t/86eyuqbbq
